---
import CoursesSignUpText from '@/layouts/function-components/CoursesSignUpText'

const {
  signUpButton: {
    signUpButton,
    signUpButtonText,
  }
} = Astro.props

---
{ signUpButton && <CoursesSignUpText content={signUpButtonText} /> }
